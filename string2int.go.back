package main

import (
	"fmt"
	"log"
	"strconv"
	"strings"
)

func main() {
	str1 := " 137"
	noSpacestr1 := strings.Replace(str1, " ", "", -1)
	i1, err := strconv.Atoi(noSpacestr1)
	if err != nil {
		log.Fatal(err)
	}

	fmt.Println(i1)
}
